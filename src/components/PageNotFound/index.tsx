import React from 'react';

import { LabelG } from '../../styles/Gstyles';
import { Container } from './styles';

const PageNotFound: React.FC = () => {
    return (
        <Container>
            <LabelG>Página não encontrada.</LabelG>
        </Container>
    );
}

export default PageNotFound;